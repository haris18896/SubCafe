import {space, sizes} from './spacing';
import {fonts, fontWeights, lineHeights, fontSizes} from './fonts';
import {
  WP,
  HP,
  RF,
  scrWidth,
  scrHeight,
  platformOrientedCode,
} from './responsive';
import DefaultPalette from './colors';

export const theme = {
  WP,
  HP,
  RF,
  space,
  sizes,
  fonts,
  scrWidth,
  scrHeight,
  fontSizes,
  lineHeights,
  fontWeights,
  DefaultPalette,
  platformOrientedCode,
};
